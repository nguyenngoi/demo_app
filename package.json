{
  "name": "iotapp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "build-android": "rm -rf node_modules/ && npm cache clean --force && npm i && cd android && ./gradlew clean && ./gradlew assembleRelease"
  },
  "dependencies": {
    "react": "16.6.3",
    "react-native": "0.58.6",
    "socket.io-client": "^2.2.0"
  },
  "devDependencies": {
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "24.1.0",
    "jest": "24.1.0",
    "metro-react-native-babel-preset": "0.53.0",
    "react-test-renderer": "16.6.3"
  },
  "jest": {
    "preset": "react-native"
  }
}
